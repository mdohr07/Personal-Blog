---
const { prevUrl, nextUrl } = Astro.props;
---


<nav aria-label="Blog Pagination">
    <hr />
    {
        prevUrl && (
            <a href={prevUrl}>
                ← NEWER ENTRIES
            </a>
        )
    }
        {
            nextUrl && (
                <a href={nextUrl}>
                   OLDER ENTRIES →
                </a>
            )
        }
</nav>

<style>
    a {
        text-decoration: none;
    }
</style>