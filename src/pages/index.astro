---
import Layout from '../layouts/Layout.astro';
const pageTitle = "mdohr";

import PostCard from '../components/PostCard.astro';
import { formatBlogPosts } from '../js/sortPosts';
const allPosts = await Astro.glob("./blog/posts/*md");
const formattedPosts = formatBlogPosts(allPosts, {
    limit: 3,
});
---
<Layout pageTitle={pageTitle} description="Frontend Development">
    
    <h1>{ pageTitle } <span>07</span> <br><small>Frontend Development</small></h1>
    <!-- <small>Frontend Developer</small> -->
   <p>Hallo 👋</p>
   <p>ich bin eine erfahrene Webentwicklerin mit einem Hintergrund in
    Kunst und Grafikdesign. Mein Schwerpunkt liegt auf 
    ästhetisch ansprechenden Front-End-Designs. <br>
    Seit 1999 entwickle ich Websites und lasse mich dabei gerne von 
    Retro-Spielen und dem "Old Web" inspirieren.
    </p>
    <p>
    Ich mag es, meine Grenzen auszutesten und professionelles sowie
    persönliches Wachstum liegen mir am Herzen. Neugierige sind herzlich 
    dazu eingeladen, auf den Button unten zu klicken 🤓.
    </p>

        <a href="/works/">
            <button>Kontakt</button>
        </a>

    <h3>Portfolio</h3>
    <p>Meine Projekte kommen hier hin</p>
    
    <!-- <p>{ description }</p> -->
    <section class="post-list" aria-label="List of blog posts">
        <h4>Neueste Blogeinträge</h4>
               {
                formattedPosts.map((post) => (
                    <PostCard 
                    frontmatter={post.frontmatter} 
                    url={post.url} />
                    ))
                }
               
    </section>
</Layout>
<style>

    /* Skill images */
        .link-images img {
            margin: 16px 4px;
        }
    @media only screen and (max-width: 600px) {
        .link-images img {
            height: 30px;
            width: 30px;
            margin: 16px 1px;
        }
    }
</style>
